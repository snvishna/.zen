#!/bin/bash
#  Zen Save v5.0 (Stow Edition)
# Snapshot & Cloud Sync

set -e

# --- Configuration ---
ZEN_HOME="${HOME}/.zen"
STOW_DIR="${ZEN_HOME}/stow"
MANIFESTS="${ZEN_HOME}/manifests"
BREWFILE="${MANIFESTS}/Brewfile"
NPM_LIST="${MANIFESTS}/npm_globals.txt"
APPS_LIST="${MANIFESTS}/installed_apps.txt"
MACOS_DIR="${MANIFESTS}/macos"
LOG_FILE="${MANIFESTS}/last_backup.log"

# Binary
BIN_SYNC="${HOME}/.local/bin/dfsync"

# --- Colors ---
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# --- Help ---
show_help() {
    echo -e "${BLUE}Zen Save - Configuration Snapshot${NC}"
    echo -e "Usage: zen-save [OPTIONS]"
    echo -e "  --all       Snapshot everything (Brew, NPM, VS Code, Apps, Defaults)."
    echo -e "  --sync      Push to cloud after snapshotting."
    echo -e "  --local     Snapshot only (Skip cloud sync)."
    echo -e "  --help      Show this help."
    exit 0
}

# --- Snapshot Functions ---
dump_brew() {
    if command -v brew &> /dev/null; then
        echo -e "🍺 Dumping Brewfile..."
        # 1. Standard Brewfile Dump
        # Suppress "Not a git repository" warnings from brew
        export HOMEBREW_NO_AUTO_UPDATE=1
        brew bundle dump --force --file="$BREWFILE" 2>/dev/null || true
        
        # 2. Append VS Code extensions to Brewfile? 
        # In previous version, logic was complex. Stow manages VSCode text file separately now.
        # So we keep Brewfile pure for Brew.
        
        echo -e "${GREEN}✔︎ Brewfile updated.${NC}"
    else
        echo -e "${RED}[SKIP]${NC} Homebrew not found."
    fi
}

dump_npm() {
    if command -v npm &> /dev/null; then
        echo -e "🟢 Dumping NPM Globals..."
        if command -v jq &> /dev/null; then
            # Safe dump: Handle empty JSON, null dependencies, or errors
            npm list -g --depth=0 --json 2>/dev/null | jq -r 'if .dependencies then .dependencies | keys[] else empty end' > "$NPM_LIST" || true
        else
            npm list -g --depth=0 | grep -v '├──' | grep -v '└──' | awk '{print $2}' | cut -d@ -f1 > "$NPM_LIST"
        fi
        echo -e "${GREEN}✔︎ NPM list updated.${NC}"
    fi
}

dump_vscode() {
    local text_file="${STOW_DIR}/vscode/.config/vscode/extensions.txt"
    if command -v code &> /dev/null; then
        echo -e "🟣 Dumping VS Code Extensions..."
        mkdir -p "$(dirname "$text_file")"
        code --list-extensions > "$text_file"
        echo -e "${GREEN}✔︎ Extensions list updated.${NC}"
    fi
}

dump_apps() {
    echo -e "📱 Dumping /Applications..."
    ls -1 /Applications > "$APPS_LIST"
    echo -e "${GREEN}✔︎ Apps list updated.${NC}"
}

dump_defaults() {
    echo -e "⚙️  Snapshotting macOS Defaults..."
    local private_macos="${MANIFESTS}/private/macos"
    mkdir -p "$private_macos"
    
    # Dump full settings to private area (Gist only, ignored by Git)
    defaults read > "${private_macos}/macos_full_dump.txt"
    echo -e "${GREEN}✔︎ Defaults exported to ${private_macos}/macos_full_dump.txt${NC}"
}

dump_private_configs() {
    echo -e "🔒 Snapshotting Private Configs..."
    local private_root="${MANIFESTS}/private"
    
    # 1. Create Directories in manifests/private
    mkdir -p "${private_root}/raycast"
    mkdir -p "${private_root}/rectangle"
    mkdir -p "${private_root}/finder"
    mkdir -p "${private_root}/launchd"
    
    # 2. Raycast (Preferences only)
    cp "${HOME}/Library/Preferences/com.raycast.macos.plist" "${private_root}/raycast/" 2>/dev/null || true
    
    # 3. Rectangle (Preferences only)
    cp "${HOME}/Library/Preferences/com.knollsoft.Rectangle.plist" "${private_root}/rectangle/" 2>/dev/null || true

    # 4. Update dfsync binary in repo
    cp "${HOME}/.local/bin/dfsync" "${STOW_DIR}/bin/.local/bin/dfsync" 2>/dev/null || true

    echo -e "${GREEN}✔︎ Private configs and binary snapshotted.${NC}"
}

run_sync() {
    echo -e "\n${BLUE}--- ☁️  Syncing to Cloud ---${NC}"
    if [[ -x "$BIN_SYNC" ]]; then
        "$BIN_SYNC" push -y
    elif command -v dfsync &> /dev/null; then
        dfsync push -y
    else
        echo -e "${RED}[ERROR]${NC} dfsync not found. Cannot push."
        exit 1
    fi
    echo -e "${GREEN}✔︎ Cloud Sync Complete.${NC}"
}

perform_snapshot() {
    local do_sync="$1"
    
    echo "========================================"
    echo "Zen Snapshot Started: $(date)"
    echo "========================================"
    
    dump_brew
    dump_npm
    dump_vscode
    dump_apps
    dump_defaults
    dump_private_configs

    if [[ "$do_sync" == "true" ]]; then
        run_sync
    else
        echo -e "\n${YELLOW}ℹ️  Skipping Cloud Sync.${NC}"
    fi
}

# --- Main ---
main() {
    local do_sync=true
    
    for arg in "$@"; do
        case $arg in
            --local|--no-sync) do_sync=false ;;
            --help|-h) show_help ;;
        esac
    done

    perform_snapshot "$do_sync" | tee "$LOG_FILE"
}

main "$@"
